fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
websockets==12.0
python-socketio==5.10.0
python-engineio==4.8.0
aiofiles==23.2.1
pypdf==5.9.0
# OCR fallback for scanned reference PDFs. Requires the tesseract system binary
# on PATH; the backend degrades to embedded-text-only without it.
pymupdf==1.28.2
pytesseract==0.3.13
Pillow>=10.0.0
# Character-sheet OCR (handwriting) is NOT installed into this environment:
# it runs in a dedicated user-created venv (requirements-ocr.txt), executed as
# a subprocess (sheet_importer.ocr_python), so paddle's dependency tree cannot
# fight FastAPI's anyio/pydantic pins and stays out of the packaged app.
