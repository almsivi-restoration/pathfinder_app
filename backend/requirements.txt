fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
websockets==12.0
python-socketio==5.10.0
python-engineio==4.8.0
aiofiles==23.2.1
pypdf==5.9.0
# OCR fallback for scanned reference PDFs. Requires the tesseract system binary
# on PATH; the backend degrades to embedded-text-only without it.
pymupdf==1.28.2
pytesseract==0.3.13
Pillow>=10.0.0
# Character-sheet OCR (handwriting) for the Pathfinder 1e sheet importer.
# Matched trio ONLY: other paddlepaddle/paddleocr/paddlex combinations hit a
# oneDNN "ConvertPirAttribute2RuntimeAttribute" executor bug or paddlex API
# mismatch. Models download to ~/.paddlex/official_models on first use; the
# packaged backend must ship them or fetch on first run (see importer notes).
paddlepaddle==3.2.2
paddleocr==3.3.0
paddlex==3.3.0
